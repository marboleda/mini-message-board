doctype html
html(lang='en')
  head
    title #{title}
    link(rel='stylesheet' href='/stylesheets/style.css')
  body   
    h1 #{title}
    ul
      each message in messages
        li
          p= message.text
          p= message.user
          p= message.added